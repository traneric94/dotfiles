# macOS-only tmux settings.
#
# (Clipboard is now handled cross-platform in tmux.conf via `set-clipboard on` +
# copy-selection-and-cancel, so the old reattach-to-user-namespace default-command
# is gone. Undercurl overrides moved to tmux.conf as a terminal capability.)
#
# Nothing macOS-specific is needed right now; kept as the home for future
# Darwin-only tweaks (sourced from tmux.conf behind a Darwin if-shell guard).
